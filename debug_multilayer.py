import pickle
import networkx as nx
import sys
import numpy as np

with open('data/processed/graphs/snapshots/snapshot_0.pkl', 'rb') as f:
    pass

# We should just run print(email_graph.subgraph(sample_nodes).number_of_edges()) inside the current logic.
